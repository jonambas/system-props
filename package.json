{
  "name": "styled-system-ts",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && node scripts/esbuild.js && tsc",
    "test": "jest --watchAll",
    "test:ci": "jest",
    "coverage": "jest --coverage && open coverage/lcov-report/index.html"
  },
  "files": [
    "dist"
  ],
  "author": "Jon Ambas",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "28.1.4",
    "esbuild": "0.14.49",
    "jest": "28.1.2",
    "rimraf": "3.0.2",
    "ts-jest": "28.0.5",
    "typescript": "4.7.4"
  },
  "dependencies": {
    "csstype": "^3.1.0"
  }
}
